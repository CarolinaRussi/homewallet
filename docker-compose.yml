services:
  postgres:
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: homewallet
      POSTGRES_PASSWORD: homewallet
      POSTGRES_DB: homewallet
    volumes:
      - homewallet_pg:/var/lib/postgresql/data

volumes:
  homewallet_pg:
